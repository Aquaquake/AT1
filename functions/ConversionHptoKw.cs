namespace AT1;
/*
public class ConversionHptoKw
{ 
    // Class Converts Horsepower to Kilowatts
    {
        private double x;
        // Private variable stores Horsepower (x)
        private double y;
        // Private variable stores Kilowatts (y)
        public int hp { get; set; }
        // Public variable sets value of Horsepower (x) 
        
        ////kW.hp = <Value>;
        
        public ConversionHptoKw(double x_)
        // Constructor inputs Horsepower (x) and initializes with value (x_)
        {
            x = x_;
        }
        public double Sum()
        // Method calculates and returns value
        {
            y = x * 0.7457;
            // Conversion for Horsepower (x) to Kilowatts (y)
            int kW = (int)Math.Round(y);
            // Rounds Kilowatts to the nearest whole number
            return kW;
            // Returns rounded value of kilowatts (kW)

            //// var kW = new ConversionHptoKw( <Value> );
            //// Console.WriteLine(kW.Sum());
        }
    }
}


add back later car.Kilowatts
*/